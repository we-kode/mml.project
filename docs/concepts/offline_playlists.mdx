---
sidebar_position: 5
---

import ThemedImage from '@theme/ThemedImage';
import useBaseUrl from '@docusaurus/useBaseUrl';

# Offline Playlists

[Clients](./clients) can stream [records](./records) fom server by filtering them by their needs or download [records](./records) into playlists.
This tracks are saved on the client device and can be listened to, even when the device has no connection to the server.

<ThemedImage
  alt="Offline Playlists"
  width="300"
  className="center"
  sources={{
    light: useBaseUrl('/img/offline_playlists_light.png'),
    dark: useBaseUrl('/img/offline_playlists.png'),
  }}
/>